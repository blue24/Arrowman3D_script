package arm;

// Yet another non-functional trait. Except nothing pays attention to this... whoops.
// This will be much better than applying the trait to hundreds of wall objects later though when some purpose is found.
class WallObject extends iron.Trait {
	public function new() {
		super();

		// notifyOnInit(function() {
		// });

		// notifyOnUpdate(function() {
		// });

		// notifyOnRemove(function() {
		// });
	}
}
