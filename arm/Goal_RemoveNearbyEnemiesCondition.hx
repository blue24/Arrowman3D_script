package arm;

// Not functional itself. Just used so that GoalArea can detect its presence on the same object to start with a condition before showing up.
class Goal_RemoveNearbyEnemiesCondition extends iron.Trait {
	public function new() {
		super();

		// notifyOnInit(function() {
		// });

		// notifyOnUpdate(function() {
		// });

		// notifyOnRemove(function() {
		// });
	}
}
